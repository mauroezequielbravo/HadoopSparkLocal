# ========================================
# Configuración de PostgreSQL (Metastore)
# ========================================

# Usuario y contraseña de PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# Usuario y contraseña para el Metastore de Hive
HIVE_DB_USER=hive
HIVE_DB_PASSWORD=hivepassword
HIVE_DB_NAME=metastore

# Puerto externo de PostgreSQL (para evitar conflictos)
POSTGRES_EXTERNAL_PORT=5433

# ========================================
# Configuración de Hadoop
# ========================================

HDFS_NAMENODE_USER=root
HDFS_DATANODE_USER=root
HDFS_SECONDARYNAMENODE_USER=root
YARN_RESOURCEMANAGER_USER=root
YARN_NODEMANAGER_USER=root

# ========================================
# Configuración de Spark
# ========================================

SPARK_MASTER_HOST=spark-master
SPARK_WORKER_CORES=2
SPARK_WORKER_MEMORY=2g
PYSPARK_ALLOW_INSECURE_GATEWAY=1

# ========================================
# Notas
# ========================================

# Este archivo es un ejemplo de configuración.
# Las variables ya están configuradas en docker-compose.yml
# Solo necesitas crear un archivo .env si quieres personalizar valores.

# Para usar este archivo:
# 1. Copia este archivo como .env
# 2. Modifica los valores según tus necesidades
# 3. Reinicia los servicios: docker-compose down && docker-compose up -d
